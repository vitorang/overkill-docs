server {
    listen 80;
    root /usr/share/nginx/html/browser;
    index index.html;

    location /assets/ {
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}