USE_SQLITE=false
USE_REDIS=true
HASHIDS_SALT=HASHIDS

SQL_PASSWORD=
SQL_USER=sa
SQL_DB=OverkillDocsDb
SQL_SERVER=sqlserver

# Se não for usar o compose, troque para o padrão abaixo
# de acordo com appsettings.json:
# FeatureFlags__UseSqlite=false